<p id="notice"><%= notice %></p>

<div class="page_body">
  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>
  
    <tbody>
      <% @buyer = @users.find_by_id(@statement.giver_id) %>
      ผู้ซื้อ
      <%=@buyer.first_name %> <%=@buyer.last_name %>
      <tr>
        <td>รายชื่อสินค้า</td>
        <td>ราคารวม</td>
        <td>pvรวม</td>
      </tr>
      <% @statement.product_statements.each do |product| %>
        <tr>
          <td><%= product.product.name %></td>
          <td><%= product.total_price %></td>
          <td><%= product.total_pv %></td>
          <td><%= link_to 'Show', product.product %></td>
        </tr>
        </td>
      <% end %>
    </tbody>
  </table>
  <%= link_to 'Edit', edit_statement_path(@statement) %> | <%= link_to 'Destroy', @statement, method: :delete, data: { confirm: 'Are you sure?' } %> |
  <%= link_to 'Back', statements_path %>
</div>

